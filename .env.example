# Production environment variables — set these in Netlify (Site settings → Environment variables).
# Do NOT commit real secrets. Copy to .env for local dev only.
# If a key was ever committed or shared, rotate it in Stripe Dashboard immediately.

STRIPE_SECRET_KEY=sk_live_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME
SITE_URL=https://krownfrontz.com

ORDER_NOTIFICATION_EMAIL=krownfrontz@gmail.com
RESEND_API_KEY=
ORDER_NOTIFICATION_FROM=orders@krownfrontz.com
RESEND_NEWSLETTER_TOPIC_ID=topic_REPLACE_ME
